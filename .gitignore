# Node & OS clutter
node_modules/
.DS_Store

# Local environment variables
.env

# Build outputs
infra/docs/schemaspy/

# The contents of these directories are generated by the workflow and should be tracked.
# The '!' is used to un-ignore them if a broader rule like 'infra/' were ever added.
!infra/dumps/
!infra/inventory/
!supabase/functions/
